document.getElementById('score').innerText = ('Score: ' + value);

function goBack() {
    window.history.back();
  }

function randomize(){
    var num = Math.random();
    if(num < 0.5){
        document.getElementById();
    }
    
}